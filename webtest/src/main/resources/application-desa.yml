spring:
  application:
    name: webtest
  profiles:
    active: mac
management:
  security:
    enabled: false
security:
  basic:
    enabled: false
  user:
    name: admin
    password: password
logging:
 config: file:/Proyectos/Apps/config/log4j2-${spring.profiles.active}.yml      